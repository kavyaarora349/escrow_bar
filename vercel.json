{
  "buildCommand": "pnpm run install:frontend && pnpm run build",
  "outputDirectory": "projects/frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
